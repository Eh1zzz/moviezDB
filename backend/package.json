{
  "name": "moviezdb-backend",
  "version": "1.0.0",
  "description": "Node.js backend for MoviezDB — bridges moviebox-api CLI to the frontend",
  "main": "server.js",
  "scripts": {
    "start":     "node server.js",
    "dev":       "nodemon server.js",
    "check-cli": "python -m moviebox_api --version || moviebox --version"
  },
  "dependencies": {
    "cors":               "^2.8.5",
    "express":            "^4.18.3",  
    "express-rate-limit": "^7.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
